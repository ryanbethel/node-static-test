{
  "name": "begin-app",
  "version": "0.0.0",
  "description": "Begin basic Hello World! app for Node",
  "scripts": {
    "start": "cross-env NODE_ENV=testing npx sandbox",
    "build": "echo created at build > ./public/at-build.ignore.txt && ls public"
  },
  "devDependencies": {
    "@architect/functions": "latest",
    "@architect/sandbox": "^3.3.6",
    "@begin/data": "latest",
    "cross-env": "^7.0.3"
  }
}